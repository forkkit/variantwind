{
  "name": "@variantwind/monorepo",
  "description": "TailwindCSS Variant Shorthands for Vue",
  "author": "Sibbngheid<https://github.com/sibbngheid>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm -C ./packages/variantwind run dev"
  },
  "devDependencies": {
    "@antfu/eslint-config-ts": "^0.2.14",
    "eslint": "^7.5.0"
  },
  "eslintConfig": {
    "ignorePatterns": [
      "**/dist/*.*"
    ],
    "extends": [
      "@antfu/eslint-config-ts"
    ]
  }
}